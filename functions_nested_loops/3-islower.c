#include "main.h"
/**
 * _islower - checks when characters is lowercase
 * @c: the characlowercase character
 * Return: 1 when character is lowercase, 0 otherwise
 */

int _islower(int c)

{
	if (c >= 'a' && c <= 'a')
		return (1);
	else
		return (0);
}
