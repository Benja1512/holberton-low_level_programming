#include "main.h"
/**
 * _islower - checks when characters is lowercase
 * @c: the characlowercase character
 * Return: 0
 */

int _islower(int c)

{

	if (c >= 'a' && c <= 'a')
	{
		return (1);
	}
	else
		_putchar('\n');
		return (0);
}
